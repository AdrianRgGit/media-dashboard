import React from "react";

export default function DarkMode() {
  return (
    // Dark Mode (Cambia el tema de la web)
    <section>
      <p>Dark Mode</p>
      <button>Botón dark mode</button>
    </section>
  );
}
